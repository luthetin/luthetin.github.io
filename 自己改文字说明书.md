# 自己改网站文字 · 说明书

> 改文字很简单：用记事本（或 VS Code）打开对应文件，找到那句话，改成你的新句子，保存，刷新浏览器即可。
> 不需要懂代码，只要遵守三条铁律（见文末）。

## 一、首页三栏卡片（「三个按钮」上的字）

文件：`index.html`（用记事本打开）

三个卡片分别在 45~64 行。每个卡片长这样：

```html
<div class="card2">
  <div class="k">自媒体</div>                    ← 卡片标题（大字）
  <div class="quote-line">在 B 站做视频，介绍词牌名</div>   ← 第二行引语
  <p>视频内容主要是讲词牌名……</p>             ← 正文（小字）
  <a class="go" href="...">去哔哩哔哩 →</a>      ← 按钮文字
</div>
```

- 想改标题 → 改 `<div class="k">自媒体</div>` 里的字
- 想改引语 → 改 `<div class="quote-line">…</div>` 里的字
- 想改正文 → 改 `<p>…</p>` 里的字
- 想改按钮文字 → 改 `<a class="go" …>去哔哩哔哩 →</a>` 里的字（`→` 可以删掉）
- 想加段落 → 在 `</p>` 后面再复制一行 `<p>新文字</p>`

### 如果你要换链接（比如 B 站换了账号）

改 `<a class="go" href="https://space.bilibili.com/431689962" …>` 里 **href=" " 引号中间**的网址。

## 二、关于页（about.html 的全部内容）

文件：`about.html`

| 位置 | 改哪里 |
|------|--------|
| 第 27 行 | `<div class="sec-title">关于我</div>` ← 页面大标题 |
| 第 28 行 | `<div class="sec-sub">周新旭 · 大一升大二</div>` ← 副标题 |
| 第 30 行 | 第一段介绍（`<p>` 开头那行） |
| 第 32~37 行 | 「一条心路」小节：箭头弧线 + 两段正文 |
| 第 39~43 行 | 「我的信条」小节：引语块 + 一段说明 |
| 第 45~46 行 | 「为什么做这个网站」小节 |

规则：
- 每段文字都在 `<p>……</p>` 之间，**把 `…` 换成你的话**，保留 `<p>` 和 `</p>` 不动
- 想整段删除：把这一整行（含 `<p>` 和 `</p>`）删掉
- 想新增一段：找一处 `</p>` 的后面，粘贴一行 `<p>你的新文字</p>`

### 关于「信条」的引语块（第 40~42 行）

```html
<div class="quote-block">
  活的潇洒，顺我自己的心，<br>而不是妥协。
</div>
```

- `…<br>…` 表示换行（在「心，」和「而」之间断开）
- 想改文字：直接改两行的内容；想去掉换行，把 `<br>` 删掉

## 三、诗歌页的两个大按钮（如果也要改）

文件：`poetry.html` 不改按钮文字——按钮名是程序从数据文件读的：
文件 `assets/js/poems.js`，开头附近：

```js
chunlian: { name: "春潋集", …
xingyin:  { name: "行吟集", …
```

改 `"春潋集"` 引号里的字，按钮就变。**注意：只改引号里面的字，别动引号和逗号。**

## 四、佳句轮播（首页粉色卡片里自动换的句子）

文件：`assets/js/quotes.js`

```js
window.SITE_QUOTES = [
  { text: "句子", attr: "出处" },
  { text: "另一句", attr: "出处" },
  …
];
```

- 想改某句 → 改 `text: "…"` 里的字
- 想改出处 → 改 `attr: "…"` 里的字
- 想加一句 → 复制一行 `{ text: "句子", attr: "出处" },` 粘贴，改内容
- 想删一句 → 删掉对应那一整行
- **注意**：每行结尾要有英文逗号 `,`（最后一行可以没有）；引号必须成对

## 五、诗歌正文、序、跋（poetry 页点开看到的内容）

文件：`assets/js/poems.js`（同一个文件）

- 结构：`body: [ "第一句", "第二句", … ]` ← 每句一行，改引号里的字
- `note: "…"` ← 这首诗的小注（留空 `""` 就不显示）
- `trans: "…"` ← **译文**（留空就不显示；填了会在诗下方显示「译文」）
- `notes: "…"` ← **注释**（同上）

想给某首诗加译文：找到这首诗的 `trans: ""`，改成 `trans: "你的译文"`。
想加注释：`notes: "你的注释"`。保存刷新即可。

## ★ 三条铁律（改坏了的 90% 都是违反这三条）

1. **只改引号里的字**（`"…"` 之间），别删引号本身，别删 `<>` 尖括号和标签
2. **中文字符随便用**，但代码符号（引号 `"`、逗号 `,`、尖括号 `<>`、等号 `=`）必须是**英文半角**的——中文全角引号「」和“”可以用来写文字，但不能用来包代码
3. **每改完一个文件就保存**，然后到浏览器按 `Ctrl+F5`（强制刷新）看效果；改坏了就 `Ctrl+Z` 撤销

## 改坏了怎么办

- 记事本里 `Ctrl+Z` 撤销
- 或者告诉我哪里不对，我来修（你尽管试，改坏了我能修回来）
